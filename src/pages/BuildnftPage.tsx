import React, { useState } from 'react';
import Buildnft from '../components/BuildnftPanel';

const BuildnftPanel = (isConnected: any, setIsConnected: any) => {
  
  return (
    <>
      <Buildnft/>
    </>
  );
};

export default BuildnftPanel;
