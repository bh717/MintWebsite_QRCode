import React, { useState } from 'react';
import OptionComponent from '../components/OptionPanel';
const OptionPanel = (isConnected: any, setIsConnected: any) => {
  
  return (
    <>
      <OptionComponent/>
    </>
  );
};

export default OptionPanel;
