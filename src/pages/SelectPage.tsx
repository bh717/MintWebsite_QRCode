import React, { useState } from 'react';
import SelectComponent from '../components/SelectPanel';
const SelectPanel = (isConnected: any, setIsConnected: any) => {
  
  return (
    <>
      <SelectComponent/>
    </>
  );
};

export default SelectPanel;
