import React, { useState } from 'react';
import TypeComponent from '../components/TypePanel';
const TypePanel = (isConnected: any, setIsConnected: any) => {
  
  return (
    <>
      <TypeComponent/>
    </>
  );
};

export default TypePanel;
